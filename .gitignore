__pycache__/
*.py[cod]
*.egg-info/
.venv/
.pytest_cache/
dist/
build/
.env
.DS_Store
*.db
# examples write metrics.json into their own dir (cwd) when run; it is a
# generated artifact the SDK copies out — never commit the stray original.
examples/*/metrics.json
