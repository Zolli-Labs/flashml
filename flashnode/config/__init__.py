"""Local policy and limits.

Host-owner-controlled configuration: resource caps, schedules, workload
class opt-ins. The agent must show exactly which limits and permissions
apply to a workload before execution.
"""
